# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.5"
metadata:
  name: cc.excelUpload.XXXnamespaceXXX
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          # Destination for BAS
          # destination: abap-cloud-abap_env-marian-zeis_dev-two-j9ir79mq-abap
          # Replace the URL with your System URL
          url: http://vhcala4hci:50000
          # Replace with your Client
          client: "001"
        credentials:
          # Make sure your password and user is in the '.env' File
          username: env:USER
          password: env:PW
        app:
          # Change your package name here, make sure the current version is included
          name: Z_EXLUP_XXXnamespaceShortXXX
          description: ""
          package: Z_EXCELUPLOAD
          transport: A4HK900057
        exclude:
          - .*\.ts
