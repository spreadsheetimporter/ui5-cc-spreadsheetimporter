{
    "name": "UI5 ExcelUpload Dev Container",
    "image": "mcr.microsoft.com/vscode/devcontainers/javascript-node:18",
    "forwardPorts": [
        4004
    ],
    "settings": {
        "terminal.integrated.defaultProfile.linux": "/bin/bash"
    },
    "runArgs": [
        "--privileged"
    ],
    "postCreateCommand": "bash -c \"sudo apt-get update && sudo apt-get install -y wget apt-transport-https gnupg2 && wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add - && sudo sh -c 'echo \\\"deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main\\\" >> /etc/apt/sources.list.d/google.list' && sudo apt-get update && sudo apt-get --only-upgrade install google-chrome-stable && google-chrome --version && sudo apt install gnome-keyring && npm i -g npm@latest && npm --version && npm i -g pnpm@latest && pnpm install --no-frozen-lockfile && npm run copyTestApps && npm run build && npm run updateChromedriver && npm i -g @sap/cds-dk\"",
    "workspaceFolder": "/workspace",
    "mounts": [
        "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached"
    ],
    "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "sapse.sap-ux-fiori-tools-extension-pack"
    ]
}