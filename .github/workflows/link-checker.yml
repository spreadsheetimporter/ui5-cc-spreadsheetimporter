name: Periodic Link Checker

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  schedule:
    - cron: '30 0 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  link-checker:
    runs-on: ubuntu-22.04
    if: "github.event.pull_request.title != 'chore: release main'"
    steps:
      - name: 'Checkout source code'
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - name: Link Checker
        id: lychee
        uses: lycheeverse/lychee-action@8646ba30535128ac92d33dfc9133794bfdd9b411 # v2.8.0
        with:
          args: --verbose --no-progress --max-concurrency 2 --exclude-loopback './**/*.md'
          output: ./lychee/out.md
          # preserve v1 behaviour: don't fail the step, let the issue-creation
          # steps below act on the exit_code (--exclude-mail was removed in
          # newer lychee; mailto links are now excluded by default)
          fail: false
          failIfEmpty: false
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
      - name: Find existing issue
        id: find_issue
        uses: micalevisk/last-issue-action@044e1cb7e9a4dde20e22969cb67818bfca0797be # v2.0.0
        if: ${{ steps.lychee.outputs.exit_code }} != 0
        with:
          state: open
          labels: |
            broken link
            automated issue
      - name: Create or update issue for broken links
        uses: peter-evans/create-issue-from-file@fca9117c27cdc29c6c4db3b86c48e4115a786710 # v6.0.0
        if: ${{ steps.lychee.outputs.exit_code }} != 0
        with:
          title: Link Checker Report
          # If issue number is empty a new issue gets
          issue-number: ${{ steps.find_issue.outputs.issue-number }}
          content-filepath: ./lychee/out.md
          labels: broken link, automated issue
